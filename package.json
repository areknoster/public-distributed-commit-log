{
  "devDependencies": {
    "@commitlint/cli": "^15.0.0",
    "@commitlint/config-conventional": "^15.0.0",
    "standard-version": "^9.3.2"
  },
  "scripts": {
    "release": "standard-version -s  --git-tag-fallback --packageFiles=[] --bump-files=[]",
    "commitlint": "commitlint --config commitlint.config.yaml --from main"
  }
}
